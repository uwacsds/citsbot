version: '3.4'

services:
  citsbot:
    build:
      context: ./
      dockerfile: Dockerfile
      target: dev
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./src:/app/src
      - ./config.dev.json:/app/config.json
